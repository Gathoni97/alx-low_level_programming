#include <stdio.h>

/**
 * main - causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("Infinite loop incomming :(\n");

	i = 0
		/**
		 * while (i < 10)
		 * {
		 * putchar(i);
		 * }
		 */
		pritf("Infinite loop avoided! \\on");

	return (0);

}
